<div class="comment__meta">
  <h4 class="comment__author fs-heading-6-base ff-heading">{{ comment.author }}</h4>
  <span class="comment__date t-subdued fs-body-75">{{ comment.created_at | time_tag: format: 'date' }}</span>
</div>
<div class="comment__content">
  {{ comment.content }}
</div>
