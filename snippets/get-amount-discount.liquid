{% liquid
  comment
    Renders a dollar discount display.

    Required parameters:
      - prod: { Object } The associated product object.
  endcomment

  if prod.price and prod.compare_at_price and prod.price < prod.compare_at_price
    echo prod.compare_at_price | minus: prod.price
  endif
-%}
